const accountId = 34569;

let name = "Sandip Mahato";

var email = "sandip.email.com";

// accountId = 23; not allowed
console.log(accountId);
/*
we use only const and let but we do not use var due to scope issue in block and functional scope
*/
console.table([accountId, name, email]);
